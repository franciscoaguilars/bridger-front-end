// import {UPDATE_APPOINTMENT, CANCEL_APPOINTMENT, CREATE_APPOINTMENT} from "../actionTypes";

// let user = JSON.parse(localStorage.getItem('user'));
// const INITIAL_STATE = user ? user : {};
// console.log(user);


// export default function resultsReducer(state=INITIAL_STATE, action) {
//   switch(action.type) {
//     case CANCEL_APPOINTMENT:
//       console.log(state.user);
//       return state.filter(appt => appt.id !== action.id);;
//     case UPDATE_APPOINTMENT:
//       return {};
//     default:
//       return state;
//   };
// };

